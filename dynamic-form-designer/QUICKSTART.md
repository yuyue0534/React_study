# 低代码表单设计器 - 快速启动指南

## 🚀 立即开始

### 1. 安装依赖
```bash
cd form-designer
pnpm install
# 或使用 npm install 或 yarn install
```

### 2. 启动项目
```bash
pnpm dev
# 或使用 npm run dev 或 yarn dev
```

### 3. 访问应用
浏览器自动打开: http://localhost:3000

## ✨ 核心特性

- ✅ 拖拽设计表单 - 无需编码
- ✅ 实时预览 - 所见即所得
- ✅ 导入导出 - JSON格式
- ✅ 角色鉴权 - 三种权限级别
- ✅ 本地存储 - 数据持久化

## 📁 项目结构

```
form-designer/
├── src/
│   ├── components/      # 组件库
│   ├── stores/          # 状态管理
│   ├── utils/           # 工具函数
│   ├── constants/       # 常量配置
│   └── pages/           # 页面
├── package.json         # 依赖配置
├── vite.config.js       # Vite配置
└── tailwind.config.js   # Tailwind配置
```

## 🎯 使用流程

1. **设计表单**
   - 从左侧拖拽组件到画布
   - 右侧编辑组件属性
   - 支持容器嵌套

2. **预览表单**
   - 点击"预览"切换模式
   - 测试表单填写
   - 点击"返回设计"继续编辑

3. **保存表单**
   - 自动保存到 localStorage
   - 手动点击"保存"按钮

4. **导出/导入**
   - 导出为JSON文件
   - 导入JSON表单

5. **测试权限**
   - 点击右上角切换角色
   - 体验不同权限功能

## 📦 生产构建

```bash
pnpm build
pnpm preview
```

## 💡 技术栈

- React 18.x + Hooks
- Tailwind CSS v3
- react-dnd
- Zustand
- Vite 5.x

---

详细文档请查看 README.md
